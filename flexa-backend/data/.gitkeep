# This directory holds preprocessed CSV files generated by the scripts:
#
#   scripts/preprocess_usda.py   → usda_clean.csv
#   scripts/preprocess_foodcom.py → foodcom_clean.csv
#
# These files are excluded from git (.gitignore) because they are large.
# Run the preprocessing scripts once before seeding the database.
