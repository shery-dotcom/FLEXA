# This directory contains trained ML model files.

# food_classifier.pt — ResNet50 Food-101 classifier (generated by scripts/train_food_classifier.py)

# food_class_map.json — class index → class name mapping

#

# These .pt files are NOT committed to git (see .gitignore).

# To generate: python flexa-backend/scripts/train_food_classifier.py
